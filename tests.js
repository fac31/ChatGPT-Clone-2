import { clearMessages } from "./public/client.js";
/*
test('Clicking send button should clear the input', () => {
    const inputElement = document.getElementById('input-text');
    const clearButton = document.getElementById('sendBtn');

    // Simulate click event on clear button
    send.click();

    // Check if input value is empty after clicking the button
    expect(inputElement.value).toBe('');
});*/